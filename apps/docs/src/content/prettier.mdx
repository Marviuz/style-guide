---
title: Prettier
summary: Prettier configuration
group: Guide
---

# @marviuz/prettier-config

This config comes with `prettier-plugin-packagejson` that allows you to format `package.json` files.

## Installation

```package-install
npm i -D @marviuz/prettier-config
```

## Usage

```js:prettier.config.mjs
import marviuzConfig from '@marviuz/prettier-config';

export default marviuzConfig;
```

## Extending the configuration

In case you want to extend the config, you can destructure the config
and add your own..

```js:prettier.config.mjs
import marviuzConfig from '@marviuz/prettier-config';

/** @type {import('prettier').Options} */
export default {
  ...marviuzConfig,
  plugins: [...marviuzConfig.plugins, 'prettier-plugin-tailwindcss'],
};
```
